#pragma once
//#include <opencv2/core.hpp>
//#include <stdio.h>
//#include <stdlib.h>
//
//
//using namespace std;
//using namespace cv;
//
//#define ALERGART_ROBUST 0
//#define ALERGART_FAST 1
//#define ALERGART_VERY_FAST 2
//#define SAVE_JPG_QUALITY 60
//
//void dumpOptions();
//
////Config_JSONfile ReadJson(string filename);
////cv::Size calculateNewImageThroughAppT(cv::Size OriginalSize, Mat transform);
//
//double calcProbOfSuccess(int n, int k, double p);
////cv::Mat run_target_detection_images(Mat im1, Mat im2, vector<float>& alg_stages_time, int type, Mat& result, string DebugInfoPath);
//cv::Mat run_target_detection_images(Mat im1, Mat im2, vector<float>& alg_stages_time, double* confidence, int type, Mat& result, string DebugInfoPath, bool dump, double LoweProb);
////cv::Mat run_target_detection_images_new(Mat im1, Mat im2, Mat H, vector<float>& alg_stages_time, double* confidence, int type, Mat& result, string DebugInfoPath, bool dump, double LoweProb);
//cv::Mat run_target_detection_images_new(Mat im1, Mat im2, Mat H, vector<float>& alg_stages_time, double* confidence, int type, double ApproxScaleThresh, double AppproxRotThresh, Mat& result, string DebugInfoPath, bool dump, double LoweProb);
//
//double calcProbOfSuccess(int n, int k, double p);
